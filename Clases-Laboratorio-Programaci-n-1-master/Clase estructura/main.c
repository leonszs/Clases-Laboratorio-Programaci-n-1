#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <alumnos.h>
#define C 5


typedef struct {

    char nombre [50];
    int nota;
    int legajo;
    int estavacio;        //0 para no  1 // para si


    }eAlumno;


inicializaralumno(l)

int main()
{
    int listaDeEnteros[5];
    eAlumno listaDeEnteros[5];
    int numero;
    eAlumno unAlumno;
    eAlumno otroAlumno;
    int i;

    for (i=0;i<C;i++)
    {
        if ()
    }






    scanf("%d",&numero);
    printf("\n \n El Numero:%d" , numero);



    scanf("%d",&unAlumno.nota);
    printf(" \n la nota del alumno es: %d",unAlumno.nota);




    scanf("%s",&unAlumno.nombre);
    printf(" \n el nombre es: %s",unAlumno.nombre);

    scanf("%s",&otroAlumno);
     printf(" \n el nombre de ese alumno es: %s",otroAlumno.nombre);

    unAlumno = otroAlumno;
    printf("%s", otroAlumno);
   // gets(unAlumno);
   // scanf("%d",&unAlumno.legajo);











    return 0;
}
mostraralumno(eAlumno listado[], int cantidad)
{
   int i;
   int TieneAlumnos=0;
   for (i=0;i<cantidad;i++)
   {

   }
}
